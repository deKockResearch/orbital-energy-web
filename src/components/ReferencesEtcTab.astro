---
import TabLayout from "../layouts/TabLayout.astro";
const items = await Astro.glob("../pages/references.md");
---

<TabLayout tabName="References, etc." id="references-tab">
  {
    items.map(({ Content }) => (
      <>
        <Content />
      </>
    ))
  }
</TabLayout>
